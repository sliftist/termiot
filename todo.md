- If we set ourselves as the default terminal, Windows doesn't like it. I believe It's something Windows is doing. It doesn't like our executable. I'm not sure if it's because it's unsigned or because it's not an installed application or what. We should figure out why and we should fix it. We already have signing for other applications, so we could sign this application. However, it makes development difficult if we need to sign it every time we want to change the default terminal. 